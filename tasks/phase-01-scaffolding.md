# Phase 1: Project Scaffolding & Toolchain

**Location**: `client/web/`
**Depends on**: Nothing
**Blocks**: All subsequent phases

## Checklist

- [ ] Initialize Next.js 16+ project with `pnpm create next-app`
  - App Router, TypeScript, Tailwind CSS, `src/` directory, import alias `@/*`
- [ ] Configure `tsconfig.json` — strict mode, path alias `@/*` → `src/*`
- [ ] Configure `next.config.ts`
  - `output: 'standalone'` for Docker
  - `images.remotePatterns` for MinIO domain (`localhost:9000`)
- [ ] Configure `tailwind.config.ts`
  - Extend with CSS variables for lavender palette
  - Add Poppins font family
- [ ] Create `postcss.config.mjs`
- [ ] Create `biome.json` — formatter (indent: 2, quotes: double) + linter (recommended rules)
- [ ] Remove ESLint config if generated by create-next-app (biome replaces it)
- [ ] Create `src/styles/globals.css` with Tailwind directives and CSS variables:
  ```
  --background: 265 33% 99%      (#FAF9FE)
  --foreground: 240 10% 10%
  --primary: 268 83% 78%          (#BC9BF3)
  --primary-foreground: 0 0% 100%
  --secondary: 268 40% 65%        (#9B7BCF)
  --destructive: 0 86% 66%        (#F25E5E)
  --success: 85 52% 51%           (#8FC93A)
  --warning: 41 100% 67%          (#FFC857)
  --muted: 265 20% 95%
  --accent: 268 83% 78%
  --card: 0 0% 100%
  --border: 265 20% 90%
  --ring: 268 83% 78%
  --radius: 0.5rem
  --sidebar-width: 260px
  --sidebar-collapsed-width: 80px
  ```
- [ ] Configure Poppins font via `next/font/google` with `cyrillic` subset in root layout
- [ ] Create `.env.example`:
  ```
  NEXT_PUBLIC_API_BASE=http://localhost:8080
  NEXT_PUBLIC_ENV=local
  ```
- [ ] Create `.env.local` (copy of example)
- [ ] Create `vitest.config.ts` — jsdom environment, path alias, setup file
- [ ] Create `vitest.setup.ts` — test globals
- [ ] Create `playwright.config.ts` — base URL `http://localhost:3000`
- [ ] Install additional dependencies:
  ```
  pnpm add @tanstack/react-query zustand react-hook-form @hookform/resolvers zod axios clsx tailwind-merge class-variance-authority lucide-react sonner
  pnpm add -D vitest @testing-library/react @testing-library/jest-dom jsdom @vitejs/plugin-react @playwright/test @next/bundle-analyzer msw
  ```
- [ ] Configure Husky + lint-staged:
  ```
  pnpm add -D husky lint-staged
  pnpm exec husky init
  ```
  - `.husky/pre-commit` runs `pnpm exec lint-staged`
  - `.lintstagedrc.json`: `{ "*.{ts,tsx,js,json,css}": ["biome check --write"] }`
- [ ] Create `components.json` for shadcn/ui (New York style, `src/components/ui`)
- [ ] Verify: `pnpm dev` starts without errors on `localhost:3000`

## Deliverables
- Working Next.js dev server
- All config files in place
- Biome linting/formatting functional
- Husky pre-commit hook active
